import { Snippet } from '../../components/snippet'
import { Field, snippet } from './demo/field'

Visual skins for `<input>` and `<textarea>` elements.

<Snippet snippet={snippet} component={Field} />