import { Snippet } from '../../components/snippet'
import { SelectUI } from './demo/select'
import snippet from '!raw-loader!obsidian.css/src/components/obsidian.select.css';


This is a pattern to change the skin of `<select>` element without losing its inherent accessibility.

<Snippet snippet={snippet} component={SelectUI} />