import { Snippet } from '../../components/snippet'
import { ImagesDemo } from '../../components/demo/images'
import images from '../../components/demo/images.css.txt'

Sets `max-width: 100%` on images and sets the fill of `<svg>` elements to be the declared color.

<Snippet component={ImagesDemo} snippet={images} />